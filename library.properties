name=SecureOTA
version=1.0.0
author=Fuzzyline-HAS2
maintainer=Fuzzyline-HAS2
sentence=HMAC-SHA256 signed secure OTA update for ESP32.
paragraph=Streams firmware from GitHub while simultaneously computing HMAC-SHA256. Commits the flash only if the signature matches — counterfeit firmware is rejected immediately. Trigger OTA on demand (e.g. on server command) without boot-time auto-update.
category=Communication
url=https://github.com/Fuzzyline-HAS2/TTGO_OTA_update_template
architectures=esp32
depends=
