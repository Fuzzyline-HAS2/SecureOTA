# ============================================================
# HMAC 비밀키 — GitHub 에 올라가면 안 됨
# ============================================================
secrets.h
scripts/secrets.py

# ============================================================
# 빌드 산출물
# ============================================================
build/
examples/*/build/
SignedOTA/build/
__pycache__/
*.elf

# ============================================================
# update.bin · update.sig · version.txt 는 제외하지 않음
# → GitHub 에 올라가야 기기가 다운로드 가능
# ============================================================

# RSA 개인키 (미사용, 안전을 위해 유지)
private_key.pem
